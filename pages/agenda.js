import React from "react";
import Layout from "../components/layout";

const Agenda = ( ) => (
    <Layout title="Full conference schedule">
        <div className="main">
            Agenda
        </div>
    </Layout>
);

export default Agenda;
